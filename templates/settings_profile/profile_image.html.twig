{% extends 'base.html.twig' %}

{% block title %}
	Photogram - Profile Settings
{% endblock %}

{% block bread %}
	→ Profile Image
{% endblock %}

{% block body %}
	{{ include('settings_profile/_settings_profile_tabs.html.twig') }}
	<div class="flex justify-center">
		<div class="block w-full md:w-1/3 mt-10 text-center">
			{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

			{{ form_label(form.profileImage, 'Upload New Image', {'label_attr': {'class': 'w-full shadow-sm border-transparent font-medium text-white bg-gray-500 hover:bg-violet-600 active:bg-violet-800 cursor-pointer rounded-full border p-4'}}) }}

			{{ form_widget(form.profileImage, {'attr': {'class': 'hidden'}}) }}
			{{ form_errors(form.profileImage) }}

			<div>
				<button type="submit" class="w-full shadow-sm border-transparent font-medium text-black bg-yellow-400 hover:bg-violet-600 hover:text-white active:bg-violet-800 rounded-md border p-2 mt-20 mb-2">Save Changes</button>
			</div>
			{{ form_end(form) }}
		</div>
	</div>
{% endblock %}
